# Requirements for Hugging Face & Replicate API Image Generator

# Core dependencies
replicate==0.22.0
requests==2.31.0
Pillow==10.1.0
customtkinter==5.2.1
tkinterdnd2>=0.4 # drag & drop — NOT the -universal fork: it imports tkinter.tix, removed in Python 3.13
python-dotenv>=0.20.0 # Added for .env support

# Hugging Face dependencies
huggingface_hub>=0.20.0  # For InferenceClient
# diffusers==0.25.0  # Temporarily commented out for local inference
# transformers==4.36.2 # Temporarily commented out for local inference
# torch>=2.2.0  # Temporarily commented out for local inference
# accelerate==0.25.0 # Temporarily commented out for local inference
# safetensors>=0.4.3 # Temporarily commented out for local inference

# Note: tkinter is part of the Python standard library and doesn't need to be installed separately
