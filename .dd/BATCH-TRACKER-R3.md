# BATCH TRACKER R3 — Omni-Image 9-item polish (2026-07-21 late)
> Living checklist, updated every step. [ ] open · [~] wip · [x] done+verified · [B] blocked

## Agent 1 — app-integrator (owns web/app.js, bridge.py, web/index.html)
- [x] 1 Library perf: tiles built ONCE per record set (Map keyed by dir+file), filter/tag-chip clicks only toggle `.hidden` — never touch `<img src>` or rebuild DOM. Initial 5k+ load chunked via requestAnimationFrame (windowed, first screen paints instantly). Verified live against the real 5420-image I:\ library: a real tag-chip click = 0 src mutations, 0 new img nodes. .dd/verify_libperf.py 14/14. app.js?v=25.
- [x] 2 Lightbox arrow-key nav: ←/→ walk prev/next in the current gallery/library order (openLightbox takes an optional nav list), clamped at both ends, ignored while typing in the tag input, Escape still closes. Verified live (real keydown events, index walked correctly + clamp proven). .dd/verify_lightbox_nav.py 8/8.
- [x] 3 Image favorites: ★ on library tiles (always-visible badge) + lightbox (own star button, synced both directions without rebuilding tiles), reuses the EXISTING "favorite" tag + add_tag/remove_tag bridge calls (no new persistence). Dedicated "favorites" filter chip (reuses .tagchip, no new CSS). Lightbox "Save as Style"/"Save as Recipe" buttons seed the save flow from that image's own metadata (state.lbMeta) — saveStylePreset()/saveRecipePreset() factored out so composer + lightbox share one implementation. Verified live end-to-end (tile→lightbox sync, lightbox→tile sync, server-side persistence re-fetch, favorites filter, cleanup left zero residue). .dd/verify_favorites.py 9/9. app.js?v=26.
- [x] 4 Tag management UI in Settings: `LibraryIndex.all_tags/rename_tag(=merge)/delete_tag/all_image_paths` (engine/library_index.py) + `vision_tagging.tag_matches/parallel_rescan_tag` (12-worker, the exact .dd/parallel_vision_backfill.py pattern) + bridge `list_all_tags/rename_tag/delete_tag/start_tag_rescan/rescan_tag_status`. New Settings > Tags panel reuses .presetrow (no new CSS). CAUGHT LIVE: PRIME's real library has 34,499 distinct tags (vision-caption phrases, near-unique per image) — would've repeated my own #1 perf bug rendered as Settings rows; fixed with a 300-row frequency cap + client-side search (same shape as the existing 40-chip tag-filter cap). Verified: .dd/verify_tag_management.py 23/23 (hermetic, mocked vision) + .dd/verify_tags_settings_ui.py 11/11 (wiring) + live E2E (real rename+revert round-trip zero residue, cap+search proven against the real 34,499-tag list). app.js?v=28.
- [x] 6-frontend Footer shows ALL services' balances at once (not just active) — bridge.get_balance() dispatches to Agent 3's per-backend balance() first (nvidia/openrouter/replicate/cliproxy/ideogram/ideogram-api), removing the now-dead duplicate inline logic those had in bridge.py. #balanceall strip fans out over all 13 services in parallel, reuses existing bal-ok/low/info/none color classes (no app.css change). Folded into the existing refreshBalance() (rides its 3 existing call sites, no new ones). Verified live: real app booted, got 13 real chips back (Fal $15.46, OpenRouter $14.24 left, Runware $19.99, Novita $10.00, rest correctly portal-only/n-a). .dd/verify_balance_footer.py 8/8. NOTE for main/final: Agent 3's item 6d (cliproxy_api.cliproxy_models()) is written but explicitly flagged "NOT wired into bridge.py/app.js yet" — that's a separate small follow-up, not done here (wasn't in my assigned item list).

## Agent 2 — theme (owns web/app.css ONLY)
- [x] 5 Rainbow/prism SELECTED buttons: font → BLACK(#0b0b0f)+600 on the 5 rules whose gradient actually paints behind text (.picker/.chip/.viewtoggle/.cmode-seg/.tagchip — all use 10%-alpha accent-soft as layer 1, so it bleeds through). `.svc button.on` deliberately left alone (solid opaque --accent layer 1 → gradient is ring-only there, already token-safe; forcing black would've dropped claret-theme contrast 5.5:1→3.57:1, a regression) — documented inline. app.css ?v=19.
- [x] 7 "Ask AI" button aesthetic alignment: pure-CSS #askaibtn restyle, no markup change needed (id already exists). app.css ?v=19.

## Agent 3 — backends-keys (owns engine/backends/{nvidia,openrouter,replicate,cliproxy,ideogram_web}_api.py + engine/keypool.py + config key-load; NOT bridge.py/app.js)
- [x] 6a NVIDIA 3-key rotation: generic `resolve_keys()`/`keypool.build()` mechanism (engine/config.py:176-193, pre-existing, not written this pass) already reads `cfg.get(config_field+"s")` per KEY_FIELDS service — for nvidia that's `nvidia_api_keys`, already a 3-item list in config.json. Verified live: `keypool.size('nvidia')==3`. jobs.py's `_run` (generic, all services) rotates on a rate-limit/auth-looking error and retries — nvidia_api.py reads `NVIDIA_API_KEY` fresh per call so a rotation takes effect.
- [x] 6b OpenRouter rotation: `openrouter_api_keys` already a 4-item list in config.json (found by a now-dead prior instance of this same agent role, before it died — I verified all 4 against the Quick Access "Working Keys (VALID)" doc, they match exactly, excludes the separate Management/Provisioning key). Verified live: `keypool.size('openrouter')==4` (better than the 2 asked for). Same generic rotation path as 6a; openrouter_api.py reads `OPENROUTER_API_KEY` fresh per call.
- [x] 6c Replicate REAL balance: RE-PROBED live 2026-07-21 (my own probe, browser UA — first attempt without a UA hit a Cloudflare bot-block 403 "error code: 1010", a false negative). `/v1/account` → 200, fields = `{type, username, name, avatar_url, github_url}`, NO balance field. `/v1/account/billing`, `/v1/billing`, `/v1/account/spend`, `/v1/account/usage` all 404. Independently confirms research/2026-07-21_provider-balance-apis.md — genuinely no $-balance API exists (PRIME's money is real, it's just dashboard-only). Kept honest portal-only in `replicate_api.balance()`, documented the Cloudflare-UA gotcha inline for future probes (bridge.py's `_VALIDATE["replicate"]` probe has no UA override → may false-negative a valid key as rejected, flagged for the integrator).
- [x] 6d cliproxy missing image models: `cliproxy_api.cliproxy_models()` + `_KNOWN_IMAGE_MODELS` already added (found on disk, uncommitted, from the dead prior instance) — live-reverified: the proxy's own 400 body on an unknown model id still lists exactly `gpt-image-1.5, gpt-image-2, grok-imagine-image, grok-imagine-image-quality` (4, up from 3 — `grok-imagine-image-quality` was the missing one). `cliproxy_models()` called live, returns all 4 confirmed-live via GET /v1/models. NOT wired into bridge.py/app.js yet (out of my scope) — function name for the integrator: `cliproxy_api.cliproxy_models() -> list[str]`.
- [x] 6e per-backend balance() functions added to all 6 owned backends, uniform `{"label": str, "kind": "ok"|"low"|"info"|"none"}` shape (matches bridge.py's existing return shape exactly) so `get_balance` can dispatch by service name:
  - `nvidia_api.balance()` → portal-only (no API exists, verified)
  - `openrouter_api.balance()` → REAL, GET /v1/credits, live-tested returns `{"label": "$14.24 left", "kind": "ok"}`
  - `replicate_api.balance()` → portal-only (no API exists, verified — see 6c)
  - `cliproxy_api.balance()` → n/a self-hosted
  - `ideogram_web_api.balance()` → n/a subscription
  - `ideogram_api.balance()` → portal-only + flags the key as currently rejected (401)
  All 6 smoke-tested live in one run (see report). None take arguments.
- [x] 8 HF keys: `huggingface_tokens` already a 6-item list in config.json (found on disk from the dead prior instance) — verified all 6 against `C:/GitHub_Projects/Quick Access/Archived API Key NFO/## Hugging Face Token (New) - 03.28*.txt` + cross-doc grep, exact match, no new keys generated. Verified live: `keypool.size('huggingface')==6`. Also fixed a real gap: jobs.py's `_RATE_LIMIT_MARKERS` never matched HF's friendly "Authorization failed. Check your token." error string (no "401" substring survives hf_api.py's wrapper) — the 6-key pool would never have rotated on a bad HF key. Added "authorization failed"/"unauthorized" markers (engine/jobs.py, one-line root-cause fix, all services benefit).
- [x] 9 Ideogram routing: VERIFIED `engine/backends/__init__.py` already correctly maps `"ideogram": ideogram_web_api.generate` (the working Firebase-web-session subscription path) and `"ideogram-api": ideogram_api.generate` (the dead API-key path, deliberately excluded from the dropdown, comment confirms). No fix needed — routing was already correct. Live-tested ONE real generation via the web pathway: submitted a prompt, polled, downloaded — got back a real 1024×1024 WebP image, spending PRIME's Plus subscription (not the dead public API balance).

## Main (me)
- [x] recall'd #9 ideogram-web pathway (built already) + located #8 HF keys + confirmed keypool exists
- [ ] final: verify all agents green, smoke, commit per area, push
- NOTE for final/Agent-2 follow-up: Agent 1's items 3 (favorite star, library tiles) and 6-frontend (#balanceall footer strip) both needed layout that app.css doesn't have a class for (always-visible tile badge; horizontal footer overflow) — inline-styled to stay in scope (app.css is Agent 2's file), documented inline in app.js/index.html. Agent 2 may want to promote these to real classes (`.favstar`, `#balanceall`) for a proper polish pass. Neither is broken as-is — both live-verified working.
- NOTE for final: Agent 3's item 6d (`cliproxy_api.cliproxy_models()`) is written but explicitly flagged "NOT wired into bridge.py/app.js yet" — small follow-up, not in Agent 1's assigned item list, not done here.
