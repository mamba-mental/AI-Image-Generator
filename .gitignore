# Environment variables (API keys)
.env
.env.*

# Live config may hold user-set API keys — track config.default.json instead
config.json

# PyInstaller
build/dist/
dist/
*.manifest

# Ignore generated images
generated_images/

# Ignore Python cache files
__pycache__/
*.pyc
*.pyo

# Logs
*.log
logs/

# IDE / Editor
.vscode/
.idea/

# OS files
desktop.ini
Thumbs.db
.DS_Store

# Build artifacts (venv + work dirs live under build/)
build/venv/
build/work*/

# Local Library caches (thumbnails + file index) — regenerated on demand
.cache/
scripts/.fal_schema_cache/
engine/fal_models.json.bak-*

# replicate legacy recovery (secrets + bulk output)
scripts/.replicate_cookie
scripts/.replicate_recover/
config.json

# secrets / backups / local data — never commit
config.json
*.bak
*.bak-*
config.json.bak*
**/config.json.bak*
.cache/
*.db
*.db-shm
*.db-wal
YOUR-RAW-PROMPTS_2026-07-13_1248.md
